syntax = "proto3";

option go_package = "genproto/users_service";

package users_service;

service AuthService{
    rpc Login(Credentials)returns(CredetialId){}
}

message Credentials{
    string login = 1;
    string password = 2;
}

message CredetialId{
    string id = 1;
}